import { ThemeConfig } from 'antd'
import { Colors } from '../../styles/Colors'

export const Page404Theme: ThemeConfig = {
  token: {
    colorTextHeading: Colors.primary,
    colorTextDescription: Colors.white
  }
}
